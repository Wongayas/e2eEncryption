<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      background-color: #f0f8ff;
      color: #111;
      font-family: Arial, sans-serif;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding-top: 40px;
    }

    h1 {
      margin-bottom: 20px;
    }

    .form_wrapper {
      display: flex;
      justify-content: center;
      align-items: flex-start;
    }

    .form_elem {
      background-color: white;
      padding: 30px;
      border-radius: 16px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
      display: flex;
      flex-direction: column;
      gap: 20px;
      min-width: 350px;
      max-width: 500px;
    }

    .password-row {
      display: flex;
      align-items: center;
      gap: 10px;
      width: 100%;
    }

    #password {
      border: 2px solid #111;
      padding: 5px;
      border-radius: 6px;
    }

    .button-column {
      display: flex;
      flex-direction: column;
      gap: 10px;
      width: 100%;
    }

    button {
      padding: 8px 12px;
      border: none;
      border-radius: 8px;
      background-color: #4682b4;
      color: white;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: #5a9bd4;
    }

    .form_elem.error input {
      border: 2px solid red;
    }

    .form_elem.success input {
      border: 2px solid #2ecc71;
    }

    .form_elem small {
      color: red;
      margin: 5px 0;
      visibility: hidden;
    }

    .form_elem.error small {
      visibility: visible;
    }
  </style>
  <body>
    <h1>File is downloading</h1>
    <form class="form_wrapper">
      <div class="form_elem" id="form_elem">
        <div class="password-row">
          <label for="password">Password</label>
          <input type="password" name="password" id="password" />
        </div>

        <div class="button-column">
          <button type="button" id="showBtn" onclick="myFunction()">
            Toggle password
          </button>
          <button type="button" id="downloadBtn">Download</button>
        </div>
        <label id="password_status" class="status-label"></label>
        <small>Password is required</small>
      </div>
      <img src="" alt="" id="wrongPswImg" />
    </form>
    <script src="/scripts/encrypt.js"></script>
  </body>
</html>
